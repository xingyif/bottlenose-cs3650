<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>

    <!-- Set the page's title. -->
    <% default_page_title = "#{params[:controller]} / #{params[:action]}" %>
    <title><%= @page_title || default_page_title %> - Bottlenose Course Manager</title>

    <!-- Set the page's favicon. -->
    <%= favicon_link_tag 'site-icon.png', :type => 'image/png' %>
    <%= favicon_link_tag 'site-icon.png', :rel => 'apple-touch-icon', :type => 'image/png' %>

    <!-- Include the page's javascripts and stylesheets. -->
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
</head>

<body style="background-color: #8b7355;">
  <div class="container text-center">
    <span class="col-sm-12 text-center">
      <%= image_tag "flammarion.png" %>
    </span>
    <h1 class="text-center">You have passed beyond the edges of this universe.</h1>
    <h5 class="text-center">So long, and thanks for all the fish.</h5>
    <p>&nbsp;</p>
    <%= image_tag "dolphin.png", style: "transform: rotate(20deg);" %>
  </div>
</body>
</html>
